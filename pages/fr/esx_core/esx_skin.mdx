import { ArgumentsTable, OutputTable } from 'components/table';

# esx_skin
The skin managing system used by `es_extended`
**Only available on clientside**

### Get Last Skin
Get the player's last known skin

```lua
AddEventHandler("esx_skin:getLastSkin", function(cb)
    cb(lastSkin)
end)
```

### Open the skin menu
Open the skin menu
**Arguments**
<ArgumentsTable rows={[
    { arg: 'submitCb', type: 'function', description: 'Callback that should be triggered when saving the skin.' }, 
    { arg: 'cancelCb', type:'function', optional: true, description: 'Callback that should be triggered when abborting.'}
]} />
